/*
Convert a numerical grade into a letter grade
*/

#include <stdio.h>

int main(void)
{
    printf("Enter you numerical grade: ");
    int numGrade1, numGrade2;
    scanf("%1d%1d", &numGrade1, &numGrade2);
    printf("The first digit is: %d ", numGrade1);
    return 0;
}